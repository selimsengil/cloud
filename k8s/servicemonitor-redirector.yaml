apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: redirector
  labels:
    release: monitoring
spec:
  selector:
    matchLabels:
      app: redirector
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
