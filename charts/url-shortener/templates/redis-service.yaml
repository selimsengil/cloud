{{- if .Values.redis.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "url-shortener.redisName" . }}
  labels:
    {{- include "url-shortener.labels" . | nindent 4 }}
    app: redis
spec:
  type: ClusterIP
  selector:
    {{- include "url-shortener.selectorLabels" . | nindent 4 }}
    app: redis
  ports:
    - name: redis
      port: {{ .Values.redis.port }}
      targetPort: {{ .Values.redis.port }}
{{- end }}
